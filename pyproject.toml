[project]
name = "Jachlam"
version = "0.0.1"
description = "An AI That Learns with what you Type."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord.py>=2.4.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
